# Registers the company service in prod. No cluster is named on purpose — the
# ApplicationSet deploys it to every cluster labelled env=prod.
app: company
env: prod
chartPath: helm/company
namespace: demo
# argoProject, not project: the cluster generator emits its own empty `project`
# parameter that would shadow this one.
argoProject: apps-prod
