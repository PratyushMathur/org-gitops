# US (us-central1) — prod clusters labelled region=us.
# Layered over helm/employee/values.yaml. Helm replaces lists, so hostnames are
# written out in full; maps like config merge.

config:
  REGION: us-central1
  CLUSTER: cluster-b

# See overrides-in.yaml.
httpRoute:
  hostnames:
    - api-us.pynd.dev
    - api-us.34.72.1.107.sslip.io

# Written by org-apps CI on every push to main (short commit sha).
image:
  tag: "b72e14d"
