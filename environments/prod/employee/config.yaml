# Registers employee in prod. Names no cluster: the ApplicationSet deploys it to
# every cluster labelled env=prod.
app: employee
env: prod
chartPath: helm/employee
namespace: demo
argoProject: apps-prod
# See company/config.yaml.
autoSync: true
