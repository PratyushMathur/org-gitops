# Consumed by the apps-prod ApplicationSet git files generator.
#
# Deployed to every cluster labelled env=prod, with no per-region overrides at
# all: each cluster gets its own identical, independent Postgres and Redis. The
# absence of an overrides-<region>.yaml here is the design, not an omission.
#
# The employee service resolves these by Service name, so the release must be named
# `regional-data` — the ApplicationSet sets releaseName from `app` below.
app: regional-data
env: prod
chartPath: helm/regional-data
namespace: demo
argoProject: apps-prod
