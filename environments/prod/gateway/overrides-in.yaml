# India (asia-south1) — prod clusters labelled region=in.
# The only per-cluster difference is which reserved IP the Gateway claims.
# `api-ind` is a google_compute_address in org-infra, so the name — not the
# address — is what travels between the repos.
gateway:
  addressName: api-ind
