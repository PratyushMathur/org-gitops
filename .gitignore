# Helm dependency artifacts. `helm dependency build` (and Argo CD's repo-server)
# rebuild these from Chart.lock, which IS committed.
helm/*/charts/

.DS_Store

# Agent scratchpad. A literal directory named "~" that gets created in the repo
# root when a tool resolves "~/tmp/..." as a relative path.
/~/
